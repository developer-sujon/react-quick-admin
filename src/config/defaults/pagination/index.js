const paginationConfig = {
  totalItems: 0,
  limit: 10,
  page: 1,
  totalPage: 0,
  sortType: "dsc",
  sortBy: "updatedAt",
  search: "",
  delay: 500,
};

export default Object.freeze(paginationConfig);
